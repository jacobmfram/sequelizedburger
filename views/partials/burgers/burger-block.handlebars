<li>
	{{burger}}

	<button class="change-eaten" data-id="{{id}}" data-neweaten="{{eaten}}">
		{{#if eaten}}NOM!{{else}}Make Another{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Remove</button>
</li>
